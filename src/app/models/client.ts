import { Car } from './car';
import { Transaction } from './transaction';

export class Client {
  $key: string;
  name: string;
  phone: string;
  email: string;
  address: string;
  // cars: Car[];
  // transactions: Transaction[];
}
